# some ls aliases
alias ls='ls --color'
alias ll='ls --color -alF'
alias la='ls --color -A'
alias l='ls --color -CF'

alias rm='rm -i'
alias mv='mv -i'

alias androidVM="$HOME/.local/android/sdk/emulator/emulator @Temp_phone"
