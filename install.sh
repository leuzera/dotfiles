#! /bin/zsh

# TODO: checar se nvim está instalado antes
sudo ln -si ~/.dotfiles/init.vim ~/.config/nvim/init.vim
# TODO: checar se zsh está instaldo antes
sudo ln -si ~/.dotfiles/zshrc ~/.zshrc
# TODO: checar se Tmux está instalado
sudo ln -si ~/.dotfiles/tmux.conf ~/.tmux.conf
# TODO: checar se prezto está instalado
sudo ln -si ~/.dotfiles/zprestorc ~/.zpreztorc
